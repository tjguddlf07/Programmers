class Solution {
  public long solution(long n) {
      double temp = Math.sqrt((double) n);
		
      if (temp % 1 == 0) {
      	long answer = (long) Math.pow(temp+1, 2);  //제곱 계산식
      	return answer;
      } else {
      	return -1;
      }
  }
}

//n이 양의 정수 x의 제곱이라면 x+1의 제곱을 리턴하고,
//n이 양의 정수 x의 제곱이 아니라면 -1을 리턴하는 함수
