class Solution {
  public long solution(long n) {
      double temp = Math.sqrt((double) n);
		
      if (temp % 1 == 0) {
      	long answer = (long) Math.pow(temp+1, 2);
      	return answer;
      } else {
      	return -1;
      }
  }
}
